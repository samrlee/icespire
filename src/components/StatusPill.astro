---
type Status = 'ally' | 'hostile' | 'unresolved' | 'at-large' | 'neutral' | 'deceased';

interface Props {
  status?: Status;
  label?: string;
}

const defaultLabels: Record<Status, string> = {
  ally: 'Ally',
  hostile: 'Hostile',
  unresolved: 'Unresolved Thread',
  'at-large': 'At Large',
  neutral: 'Neutral',
  deceased: 'Deceased',
};

const { status = 'neutral', label } = Astro.props;
---

<span class:list={['status-pill', status]}>{label ?? defaultLabels[status]}</span>
